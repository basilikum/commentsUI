import { ClickIfDirective } from './click-if.directive';

describe('ClickIfDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickIfDirective();
    expect(directive).toBeTruthy();
  });
});
