import { AutoLinksPipe } from './auto-links.pipe';

describe('AutoLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
