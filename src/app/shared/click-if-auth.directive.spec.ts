import { ClickIfAuthDirective } from './click-if-auth.directive';

describe('ClickIfAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickIfAuthDirective();
    expect(directive).toBeTruthy();
  });
});
